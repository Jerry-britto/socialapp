import 'package:flutter/material.dart';

class SearchVideo extends StatelessWidget {
  const SearchVideo({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Display videos"),);
  }
}